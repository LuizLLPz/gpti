export type Checklist = {
    id?: number;
    projectID: number;
    name: string;
    description: string;
}

export type ChecklistDTO = {
    ID?: number;
    IDPROJETO: number;
    NOME: string;
    DESCRICAO: string;
}