export type User = {
    username: string;
    name?: string;
    surname?: string;
    password: string;
}

export type UserDTO = {
    NOMEUSUARIO: string;
    NOME?: string;
    SOBRENOME?: string;
    SENHA: string;
}